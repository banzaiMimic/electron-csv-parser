{
  "name": "electron-csv-parser",
  "version": "1.0.0",
  "description": "csv parser boilerplate using electron",
  "main": "src/main.js",
  "repository": "git@github.com:dr-mimic/electron-csv-parser.git",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . csv-parser --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds"
  },
  "devDependencies": {
    "electron": "^11.2.1",
    "electron-packager": "^15.2.0"
  },
  "dependencies": {
    "exceljs": "^4.2.0"
  }
}
